# Test comb — 2026-08-29 night shift

All 72 test files read in full and cross-checked against lib by four parallel
audit agents. `flutter analyze`: clean. Suite-wide: no `skip:` flags, no
commented-out tests, no swallowed exceptions, no mock-in/mock-out theatre.
Overall: the suite is honest. 2 files stale, 3 dishonest *tests* (not files),
the rest honest or honest-but-thin.

## Stale — would fail or lie against current lib

- app/test/ui/cookbook_view_test.dart:97-98,133 — asserts `RecipeCover findsNothing`
  in list view; since 2026-08-28 every list row draws a mini cover thumb
  (app/lib/ui/recipe_list_screen.dart:602-625). These asserts contradict lib —
  expected RED in the full suite.
- app/test/data/saf_store_test.dart:84 — test named "unreadable child counts
  skipped, never fatal" no longer exercises that claim; body was rewritten for
  the batch-read refactor (seeds a directory, asserts skipped == 0). The real
  unreadable-file branch (saf_store.dart:143-145, bytes == null → skipped++)
  has NO test on the recipe-store side (pantry twin covers its own).
- app/test/data/sync_source_test.dart:4 — header comment says "nothing outside
  root/images"; code and tests enforce five shapes. Comment-only rot.

## Dishonest — greens that assert nothing real

- app/test/domain/recipe_tag_test.dart:71-76 — "the catalog halves agree —
  every key has an icon and vice versa" only checks kTagIconKeys for
  duplicates. The bindings (kFoodIcons, app/lib/ui/icons/food_icons.dart:20)
  are never tested anywhere; a key missing its icon renders kUnknownTagIcon
  with the suite green — the exact trap the test's comment claims to guard.
- app/test/l10n/arb_parity_test.dart:77-83 — not-offered-locale "reporting how
  far behind" asserts `missing.length <= templateKeys.length`, a set-difference
  tautology; printOnFailure never fires. 10 permanent greens asserting nothing.
- app/test/domain/product_categories_test.dart:96-106 — "every mapped value is
  a canonical category" samples 5 tags; the ~80 private map entries are never
  swept, so a typo'd value in any unsampled entry passes.

## Caveats — green that must not be over-cited

- starter_foods_test.dart pins kcal/fiber/serving-weight literals that are
  transcription echoes of the same UNVERIFIED table (starter_foods.dart:8).
  Green = no accidental edits + EU-carbs arithmetic. Green ≠ USDA-verified.
  When the table is verified/patched, these literals break by design.
- arb_parity green ≠ translations complete: completeness (en ⊆ locale)
  currently binds zero locales (kOfferedLanguages = system + english; en is
  skipped). Only invented-keys + @@locale are enforced for the 10 translations.
- unlock_tab_test pins the disabled placeholder truthfully (CTA onPressed null,
  "nothing to buy just yet"). It cannot catch a broken unlock — none exists —
  and will trip the moment billing wires the button. Honest for an unstarted seam.

## Coverage holes — zero tests anywhere

- lib/data/saf_diary_store.dart (171 lines) — the diary store actually used
  once a folder is picked; only LocalDiaryStore is tested.
- GeminiExtractor.extractContentFromText (:88-96, the link AI fallback) and
  extractLabel (:106-120) — link_extractor_test stubs the fallback closure, so
  the real prompt/plumbing never runs in test.
- lib/data/recipe_pdf.dart (247 lines) and lib/data/drive_docs.dart (193
  lines) — no test file at all.
- nutritionWords (lib/domain/nutrient_display.dart:115-146) — the "~" prefix /
  per-serving headline / "Estimated from N of M" honesty note shared by badge
  and PDF; untested.
- The redrawn cookbook tag shelf — alphabetical sections, untagged flat above,
  fold persistence via cookbookOpenSections (recipe_list_screen.dart:104-162,
  227-268) — no test mounts it; only the generic CollapsibleShelf is tested.
- Settings screens Meals / Tags / Diary goal — engines covered in model tests;
  the three screens themselves have no widget test.
- tag_editor_screen: create half tested; edit mode, delete, adopting name-lock,
  duplicate-name refusal, grid-tint (:326) unexercised.
- Diary card provenance corner badge (diary_tab.dart:551-569) — photo asserted,
  badge never (find.byIcon(Icons.kitchen_rounded) would do it).
- Minor: app_settings rows (cookbookView/units/language/mealNames/goals/crash
  flag), product copyWith clear-flags, label_read valid-confidence + exact
  ceiling boundaries, trends mid-loop range taps (no-crash only, honestly
  scoped).

## Per-file verdicts

data (27): all honest except saf_store_test (stale name + lost branch).
Thin: app_settings, diary_store (SafDiaryStore), gemini_extractor (text/label).
domain (16): all honest except recipe_tag (finding above); caveats on
starter_foods + arb_parity; thin: nutrient_display, product_roundtrip
(clear flags), label_read (minor).
ui (29): all honest except cookbook_view (stale + thin). Thin: settings_tab,
tag_editor_screen, trends_flow (declared smoke), diary_flow (badge).
version_sync: honest — reads pubspec at runtime vs kAppVersion, 0.18.3 pinned;
build number has no lib mirror to pin.
Helpers/fixtures: no rot; fake_saf_channel carries its own contract-fidelity
test; the three fixture JSONs' staleness is itself the tested behavior.

## Suggested repairs (proposals only — Arnar decides)

1. cookbook_view: fix the three cover asserts to the thumb-drawing reality.
2. saf_store: restore the unreadable-file branch test via unreadableIds hook;
   rename or split the mislabeled test.
3. recipe_tag: assert kFoodIcons covers kTagIconKeys both ways.
4. arb_parity: delete the tautology test or make it a real completeness report.
5. product_categories: sweep all map values via a public getter or reflection
   of the full tag list, not 5 samples.
