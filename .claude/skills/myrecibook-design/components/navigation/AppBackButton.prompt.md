The one back button — `arrow_back` at 22px (the app-bar icon size) in a 44px round target. Use on flat app bars; over image heroes use `GlassCircle icon="arrow_back"` instead.

```jsx
<AppBackButton onClick={goBack} />
```
