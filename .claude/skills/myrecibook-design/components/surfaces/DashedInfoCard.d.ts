/**
 * Dashed-outline card for quiet product promises. No fill, no shadow —
 * deliberately the least card-like card.
 */
export interface DashedInfoCardProps {
  /** The promise copy, 12.5px on-surface-variant */
  children?: React.ReactNode;
  style?: React.CSSProperties;
}
export declare function DashedInfoCard(props: DashedInfoCardProps): JSX.Element;
