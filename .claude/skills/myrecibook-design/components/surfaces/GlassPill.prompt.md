Frosted-glass stadium pill — the provenance flipper on the recipe hero ("original" ⇄ "cover").

```jsx
<GlassPill icon="swap_horiz" label="original" onClick={flip} />
```

Sits bottom-right over the hero image. Same glass recipe as GlassCircle (12px blur + hairline).
