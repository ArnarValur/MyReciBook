40px frosted-glass circle button — floats over image heroes (recipe detail: back / edit / favorite / delete).

```jsx
<GlassCircle icon="arrow_back" />
<GlassCircle icon="favorite" fill iconColor="var(--tertiary-container)" />
```

12px blur, translucent fill, hairline glass border. Only over imagery/heroes — on flat surfaces use a plain surface-container-high circle instead.
