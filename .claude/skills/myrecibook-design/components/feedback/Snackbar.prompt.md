Floating snackbar — inverse-surface pill-adjacent card (radius 12), 20px margins, elev-2. Positioned `absolute`, mount inside the screen frame.

```jsx
<Snackbar message="Notes saved" />
<Snackbar message="Screenshot saved for your next import" />
```

Copy register: calm receipts stating exactly what happened — "Added to grocery — checked-off items skipped", never "Success!".
