The ONE pantry product card — Pantry list and the diary's Add-food picker share it verbatim; never build a lookalike.

```jsx
<ProductRow name="Bell Pepper" brand="Coop" quantity="3 stk" kcal={26} />
<ProductRow name="Spaghetti" quantity="500 g" kcal={358} image={photo} />
```

38px photo thumb (radius 12); no photo → secondary-container tile with a `kitchen` icon. Name is body-lg w600, meta joins brand · quantity in body-sm muted, kcal rides a MetaChip. Stack with 8px gaps.
