Tiny tracked uppercase section label — the ONLY ALL-CAPS text in the app (the component uppercases for you; don't type caps into a string). Sits 12px above its card.

```jsx
<SectionLabel>Ingredients · 8</SectionLabel>
<SectionLabel trailing={<span>…pin pill…</span>}>Asian pantry · 1</SectionLabel>
```

Counts use a middot (`Produce · 2`). 11px Inter w600, +0.9px tracking, on-surface-variant.
