Stadium metadata chip — recipe facts under the title (time, servings).

```jsx
<MetaChip icon="schedule" label="25 min" />
<MetaChip icon="restaurant" label="Serves 4" />
```

Icon is always primary; fill is surface-container-high. Chips wrap with 8px gaps. Show the recipe's raw strings verbatim ("25 min", "4–6 servings") — never reformat them.
