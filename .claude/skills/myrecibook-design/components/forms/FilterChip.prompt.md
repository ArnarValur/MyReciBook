36px stadium filter chip — cookbook filters (All / Favorites / Quick / Sweet). Selected = secondary-container.

```jsx
<FilterChip label="All" selected />
<FilterChip label="Favorites" icon="favorite" />
```

Chip rows scroll horizontally and bleed off the right screen edge (the cut chip is the scroll cue). 8px gap between chips.
