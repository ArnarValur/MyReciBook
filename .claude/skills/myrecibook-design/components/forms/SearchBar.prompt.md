48px pill search field on surface-container — the cookbook's live title filter.

```jsx
<SearchBar value={q} onChange={setQ} />
```

Placeholder text is on-surface-variant, body-lg (14px). No border, no shadow — the tonal fill is the affordance.
